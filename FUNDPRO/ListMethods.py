numbers = [21, 12, 11]
numbers.append(20) # appends a list on the last element
numbers.insert(0,15) # inserts a value in any index of the list
numbers.remove(11) # removes a value
# numbers.clear() # clears the list
# numbers.pop() # removes the last element of the list
# numbers.count() #counts the occurences of the element in the list
# numbers.sort() #sorting the lists in ascending order
# numbers.reverse() #sorting the lists in reverse order
# numbers2 = numbers.copy() # copies the original list into a new list
print(numbers)
print(numbers.index(21)) #Displays the index of the value